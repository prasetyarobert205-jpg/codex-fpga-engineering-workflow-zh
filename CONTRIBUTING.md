# 贡献指南

欢迎提交 Issue 和 Pull Request。

1. 先说明工作流缺口、真实证据和适用边界；
2. 保持角色范围明确，不能破坏单一产品写入者；
3. reviewer 保持只读，验证资产作者不能自签；
4. 结构修改同步更新 `scripts/validate-package.ps1` 和相关 smoke；
5. 不提交私人路径、客户事实、器件专属假设、厂商数据库或无报告支持的 PASS 声明；
6. 文档使用简体中文；HDL、命令、schema key、状态码和通用术语可保留英文；
7. 提交前运行包验证，并在 PR 中记录命令、环境、结果和剩余 `UNVERIFIED`；
8. 不得为了 CI 变绿删除失败测试或降低 CDC/RDC、STA、电气、安全、许可证、单写入和独立审核门禁。

任何弱化接口、时钟/复位、CDC/RDC、电气、安全、许可证或独立签核的变更，都需要明确理由和维护者审核。
