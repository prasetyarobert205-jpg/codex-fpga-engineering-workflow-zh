# FULL 示例

```text
使用 $run-fpga-workflow，以 FULL 模式实现一个异步 streaming datapath。
先确认 port、clock/reset 关系、throughput、latency、FIFO、backpressure、error recovery、target 和 acceptance evidence。
冻结限定 impact cone 和 cycle contract；按需调用 CDC/timing、verification、vendor 和 Shadow temporal review；保持一个产品写入者；Codex 过程文件进入 codex_out；集成 diff 后重新专项复核并获得独立签核。
不要猜测缺失硬件事实。
```
